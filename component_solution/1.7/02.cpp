#include <iostream>
#include <string>
#include <vector>
#include <utility>

using namespace std;

vector< pair<char, int> > vec;

int main() {
    string str;
    int ans = 0;
    cin >> str;
    for(string::iterator iter = str.begin(); iter != str.end(); iter++) {
        
        if(_iter == vec.end()) {

        }
    }
    cout << ans << endl;
    return 0;
}
